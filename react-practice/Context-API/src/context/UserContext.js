import React from "react";

const UserContext = React.createContext();

export default UserContext;


// har ek context is a provider  , kind of wrapper 
{/* <UserContext>
    <Long>
        <card>
    <Long/>
<UserContext/> */}

// through this , the block of code can access the global variables of context